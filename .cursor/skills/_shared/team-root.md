# team → root_id

> **SSOT**: `domain-knowledge/jira/team-roots.json`  
> Open-source default ships **one** `demo` team. Add more keys under `teams` for 1..N product lines.

| team | root_id | display_name |
|------|---------|--------------|
| demo | 100001 | Demo Product Team |

Aliases: `default`. Also `root-id=100001` or `board_id=1`.

Optional `defaults.default_team` (currently `demo`).
