# curated/

Pipeline output for recognized modules, aggregates, and adjudicated briefs (`_deliver/`).

This directory is **empty in the public tree**. Populate by running:

```bash
# after Confluence sync + human 确认
@distill-domain-knowledge <root-id>
# or continue after @generate-knowledge-from-wiki
```

Do not commit production customer domain briefs to a public fork.
